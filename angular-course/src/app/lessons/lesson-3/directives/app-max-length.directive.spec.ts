import { MaxLengthDirective } from './app-max-length.directive';

describe('AppMaxLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxLengthDirective();
    expect(directive).toBeTruthy();
  });
});
