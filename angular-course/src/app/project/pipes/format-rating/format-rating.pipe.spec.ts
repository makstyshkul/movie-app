import { FormatRatingPipe } from './format-rating.pipe';

describe('FormatRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
